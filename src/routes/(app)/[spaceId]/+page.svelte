<script lang="ts">
	// let nameInput = $state('');
	// $effect(() => {
	// 	nameInput = g.space?.name || '';
	// });

	// async function createChannel() {
	// 	if (!g.space) return;

	// 	const channel = await roomy.create(Channel);
	// 	channel.name = 'channel';
	// 	channel.commit();

	// 	g.space.channels.push(channel);
	// 	g.space.commit();
	// }

	// function saveInfo() {
	// 	if (!g.space) return;
	// 	g.space.name = nameInput;
	// 	g.space.commit();
	// }
</script>

<div class="flex w-full flex-col">
	<!-- <form class="mx-auto">
		<fieldset class="fieldset">
			<legend class="fieldset-legend">Space Name</legend>
			<input bind:value={nameInput} type="text" class="input" placeholder="My Space" />
		</fieldset>

		<button class="btn" onclick={saveInfo}>Save</button>
	</form>

	<button class="btn btn-xl m-auto" onclick={createChannel}>Create Channel</button> -->
</div>
